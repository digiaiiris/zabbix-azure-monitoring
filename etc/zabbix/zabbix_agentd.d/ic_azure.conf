# Azure metric retrieval (configuration file, resource group, provider name, resource type, resource name, timeshift, metric, statistic, timegrain)
UserParameter=azure.metric.timeshift[*],azure_metric -c "$1" -g "$2" -p "$3" -t "$4" -r "$5" --timeshift "$9" "$6" "$7" "$8"

# Azure metric discovery (configuration file, resource group, provider name, resource type, resource name)
UserParameter=azure.discover_metrics[*],azure_discover_metrics -c "$1" -g "$2" -p "$3" -t "$4" -r "$5"

# Azure resource discovery (configuration file)
UserParameter=azure.discover_resources[*],azure_discover_resources -c "$1"
