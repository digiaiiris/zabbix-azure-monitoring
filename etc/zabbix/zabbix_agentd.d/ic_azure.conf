# Azure metric retrieval (configuration file, resource group, provider name, resource type, resource name, timeshift, metric, statistic, timegrain)
UserParameter=azure.metric[*],azure_metric -c "$1" -g "$2" -p "$3" -t "$4" -r "$5" "$6" "$7" "$8"
UserParameter=azure.metric.timeshift[*],azure_metric -c "$1" -g "$2" -p "$3" -t "$4" -r "$5" --timeshift "$9" "$6" "$7" "$8"
UserParameter=azure.metric.roleinstance[*],azure_metric -c "$1" -g "$2" -p "$3" -t "$4" -r "$5" -i "$6" "$7" "$8" "$9"
UserParameter=azure.metric.rolename[*],azure_metric -c "$1" -g "$2" -p "$3" -t "$4" -r "$5" -n "$6" "$7" "$8" "$9"

# Azure metric discovery (configuration file, resource group, provider name, resource type, resource name)
UserParameter=azure.discover.metrics[*],azure_discover_metrics -c "$1" -g "$2" -p "$3" -t "$4" -r "$5"

# Azure instance retrieval (configuration file, resource group, provider name, resource type, resource name, metric)
UserParameter=azure.discover.roles[*],azure_discover_roles -c "$1" -g "$2" -p "$3" -t "$4" -r "$5" "$6" "$7"

# Azure resource discovery (configuration file)
UserParameter=azure.discover.resources[*],azure_discover_resources -c "$1"
